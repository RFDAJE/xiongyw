run kvm.sh as root (or sudo).


this script is to create kvm domains (virtual machines) according to the
configuration specfied in the settings.sh

it's assumed that the TOOLS node (local repository, dnsmasq) is configured already.
